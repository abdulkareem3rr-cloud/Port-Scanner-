# Port-Scanner-
 	Accepts target IP/domain and port range
• 	Uses SOCKET to check for open ports
• 	Optionally integrates NMAP or SCAPY for deeper service detection
• 	Displays results with port number, status (open/closed), and service name (if detected)
• 	Logs output to a text or CSV file
